# Species supported by the FRTC 2025 equations

Returns the accepted species codes and scientific names used by
\[frtc_total_biomass()\].

## Usage

``` r
frtc_species()
```

## Value

A data frame with \`species_code\` and \`scientific_name\`.
